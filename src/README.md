## Prequisites
- `ImageMagick` and `bash` or `zsh` for convert the `.pdf` file to image.
- [uv](https://docs.astral.sh/uv/getting-started/installation/)
## How to run 
1. Install the dependency: `uv sync`
2. Run: 